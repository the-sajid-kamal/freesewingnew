import { front as titanFront } from '@freesewing/titan'

export const front = {
  from: titanFront,
  name: '{{ name }}.front',
  hide: { from: true },
  measurements: [],
  options: {},
  draft: ({ part }) => {
    return part
  },
}
